import { PageWrapper } from '@/components/layouts/page-wrapper';

const PortfolioScreen = () => {
  return <PageWrapper title="Detail">Detail portfolio</PageWrapper>;
};

export default PortfolioScreen;
